(function (element, $) {
  'use strict';
  var target = $(element),
    placeholder = "Search for document that you are looking for....";
  target.attr('placeholder', placeholder);

}('.cmp-search--landing-hero input', jQuery));
